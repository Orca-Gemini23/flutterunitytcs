image: ghcr.io/cirruslabs/flutter
stages:
  - androidBuild

android:
  stage: androidBuild
  script:
    - flutter clean 
    - flutter pub get
    - flutter build appbundle --build-number ${CI_JOB_ID:0:8}

